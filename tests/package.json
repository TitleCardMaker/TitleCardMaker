{
  "name": "titlecardmaker-webui",
  "version": "1.0.0",
  "description": "Automated Title Card creator for Plex, Jellyfin, and Emby.",
  "main": "index.js",
  "scripts": {
    "cy:run": "cypress run --browser chrome",
    "cy:run:report": "cypress run --browser chrome --record || exit 0",
    "cy:run:report:html": "cypress run --browser chrome ; npx mochawesome-merge cypress/results/*.json > cypress/results/output.json && npx marge cypress/results/output.json --reportDir cypress/results/ --inline",
    "cy:open": "cypress open"
  },
  "author": "Collin Heist",
  "devDependencies": {
    "cypress": "^14.3.0",
    "mocha": "^10.6.0",
    "mochawesome": "^7.1.3",
    "mochawesome-merge": "^4.3.0"
  }
}
